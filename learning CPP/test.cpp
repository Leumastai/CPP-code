#include "bank_mgt_sys.hpp"

/* #include <iostream>


using namespace std;

int main()
{
    //BankAccount ac;
    //ac.createAccount();
    //cout << name;
    //cout << name << endl; 

    string str1;
    char identifier;

    cin >> j;
    cout << j << endl;
    //cin >> identifier;
    //identifier = toupper(identifier);
    for(;;)
    {
        cout << "Please enter a valid identifier! " << endl;
        std::cin >> str1;
        getline(std::cin, identifier);
        std::cin >> identifier;// >> std::endl;
        //identifier = toupper(identifier); 
        cout << identifier;
        //j = identifier;
        if (identifier == 'S');// || identifier == 'C');
            break;
    } 


    return 0;
} */